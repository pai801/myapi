name: Render Keep-Alive
on:
  schedule:
    - cron: "*/5 * * * *" # 每 5 分钟一次（UTC）
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render myapi
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://myapi.onrender.com/
